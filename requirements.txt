# Mirrors pyproject.toml's [project.dependencies] + [project.optional-dependencies].dev.
# pyproject.toml is the source of truth (pip install -e ".[dev]" is the primary install
# path) — this file exists for tools/workflows that expect a plain requirements.txt.
#
# Regenerate after changing pyproject.toml with:
#   pip install -e ".[dev]" && pip freeze --exclude-editable > requirements.txt
# (then re-add this header, since pip freeze won't preserve it)

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pydantic>=2.7
pydantic-settings>=2.3
python-jose[cryptography]>=3.3
bcrypt>=4.1
python-multipart>=0.0.9
email-validator>=2.1
redis>=5.0
celery>=5.4
httpx>=0.27
pillow>=10.4

# Dev tools (tests, linting) — omit these for a production install.
pytest>=8.2
pytest-asyncio>=0.23
ruff>=0.5
