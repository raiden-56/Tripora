import { MarketingSite } from "../marketing/MarketingSite";

// Public marketing website — routes users into the existing app's auth/demo entry points.
export default function Landing() {
  return <MarketingSite />;
}
