import { Body, Controller, Get, Patch, Req, UseGuards } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { JwtGuard } from '../common/auth'; import { User, UserDocument } from '../auth/user.schema'; @Controller('users/me') @UseGuards(JwtGuard) export class UsersController { constructor(@InjectModel(User.name) private users: Model<UserDocument>) {} @Get() async get(@Req() req: any) { return { success: true, data: await this.users.findById(req.user.sub) }; } @Patch() async update(@Req() req: any, @Body() body: any) { delete body.passwordHash; delete body.role; return { success: true, data: await this.users.findByIdAndUpdate(req.user.sub, { $set: body }, { new: true }) }; } }
