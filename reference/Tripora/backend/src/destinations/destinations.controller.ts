import { Controller, Get, Query } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { Destination, DestinationDocument } from './destination.schema'; @Controller('destinations') export class DestinationsController { constructor(@InjectModel(Destination.name) private destinations: Model<DestinationDocument>) {} @Get() async list(@Query('q') q?: string) { const filter = q ? { $text: { $search: q } } : {}; return { success: true, data: await this.destinations.find(filter).sort('-popularity').limit(50).lean() }; } }
