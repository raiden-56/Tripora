import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { HydratedDocument } from 'mongoose'; export type DestinationDocument = HydratedDocument<Destination>; @Schema({ timestamps: true }) export class Destination { @Prop({ required: true, index: 'text' }) city: string; @Prop({ required: true, index: 'text' }) country: string; @Prop({ index: 'text' }) region?: string; @Prop() description?: string; @Prop() image?: string; @Prop({ default: 0 }) costIndex: number; @Prop({ default: 0, index: true }) popularity: number; @Prop({ type: [String], default: [] }) tags: string[]; } export const DestinationSchema = SchemaFactory.createForClass(Destination);
