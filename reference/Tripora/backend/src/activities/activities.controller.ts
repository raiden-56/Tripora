import { Controller, Get, Query } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { Activity, ActivityDocument } from './activity.schema'; @Controller('activities') export class ActivitiesController { constructor(@InjectModel(Activity.name) private activities: Model<ActivityDocument>) {} @Get() async list(@Query('q') q?: string) { const filter = q ? { $text: { $search: q } } : {}; return { success: true, data: await this.activities.find(filter).sort('-rating').limit(100).lean() }; } }
