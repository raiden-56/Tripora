import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose'; import { Types, HydratedDocument } from 'mongoose'; export type ActivityDocument = HydratedDocument<Activity>; @Schema({ timestamps: true }) export class Activity { @Prop({ type: Types.ObjectId, ref: 'Destination', index: true }) destinationId: Types.ObjectId; @Prop({ required: true, index: 'text' }) name: string; @Prop({ index: 'text' }) description?: string; @Prop({ index: true }) category: string; @Prop({ default: 0 }) duration: number; @Prop({ default: 0, min: 0 }) estimatedCost: number; @Prop({ default: 0, index: true }) rating: number; } export const ActivitySchema = SchemaFactory.createForClass(Activity);
